我将更新“洞察与诊断”模块的摘要信息卡片，将其调整为用户要求的 5 个核心维度，并增加悬浮查看细分项数据的功能。

### 实施计划

1.  **修改数据逻辑 (`src/pages/InsightDiagnosis.tsx`)**:
    *   重构 `getMetrics` 函数，使其返回符合要求的 5 个核心指标数据：
        1.  **AI 品牌指数** (综合加权得分)
        2.  **AI 搜索热度** (新增，模拟数据)
        3.  **提及率** (现有，补充细分项)
        4.  **情感度** (现有，补充细分项)
        5.  **信源分数** (新增，模拟数据)
    *   为每个指标构建详细的细分项数据结构（如：搜索热度下的“行业热门搜索”、“品牌热门搜索”等）。

2.  **封装指标卡片组件**:
    *   创建一个新的组件或在文件内定义渲染函数，用于展示单个指标卡片。
    *   实现 **Hover/悬浮交互**：当鼠标悬停在卡片上时，显示包含细分项列表的浮层（Tooltip）。

3.  **更新界面布局**:
    *   将原有的 4 个卡片布局改为 5 个卡片的网格布局。
    *   应用现有的设计风格（红/白配色，Lucide 图标），保持界面一致性。
    *   确保细分项展示清晰，包含标题和对应的值。

4.  **验证**:
    *   确认 5 个指标卡片显示正常。
    *   确认 AI 品牌指数是基于后四项的加权计算结果。
    *   确认鼠标悬停时能正确显示各指标对应的细分项详情。
