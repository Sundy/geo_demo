我将在“洞察与诊断”页面的 5 个核心指标卡片下方，新增一个名为 **“AI搜索热度详情分析”** 的区域。

### 实施计划

1.  **新建组件 `src/components/SearchHeatAnalysis.tsx`**:
    *   该组件将专门用于展示“AI搜索热度”维度的详细数据。
    *   **布局设计**:
        *   **上半部分（3列布局）**: 分别展示 **行业热门搜索**、**品牌热门搜索**、**竞品热门搜索**。
            *   每列为一个列表，包含表头（问题、核心意向词、热度）和 Top 榜单数据。
        *   **下半部分（2列布局）**:
            *   **左侧**: **热门城市分布**（柱状图），展示热度前 20 的城市。
            *   **右侧**: **热门周期趋势**（折线图），展示时间段内的热度波峰波谷。

2.  **集成到 `InsightDiagnosis.tsx`**:
    *   在 `Global Core Metrics`（5个卡片）下方，`InsightCharts`（原有图表）上方，插入这个新的 `SearchHeatAnalysis` 组件。
    *   这将作为详情分析的“第一个区域”。

3.  **数据模拟**:
    *   在组件内部生成符合业务场景的 Mock 数据（如新能源汽车相关的搜索词、城市数据等）。
    *   使用 `recharts` 库来实现柱状图和折线图，保持与现有图表风格一致。

4.  **样式风格**:
    *   沿用现有的红/白配色方案和卡片圆角风格。
    *   列表将使用紧凑设计以展示更多信息。

### 预期效果
页面结构将变为：
1.  筛选栏
2.  5个核心指标卡片 (摘要)
3.  **[新增] AI搜索热度详情分析区域** (列表 + 城市/周期图表)
4.  原有图表及其他内容...
